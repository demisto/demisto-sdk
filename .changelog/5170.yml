changes:
- description: Added LLM configuration fields under promptConfig object (temperature, maxOutputTokens, webSearch) to Script schemas for AI Tasks with automatic defaults.
  type: feature
pr_number: 5170
