changes:
- description: >
    Fixed `demisto-sdk run --debug` crashing when output contained stack traces
    (caused by `<module>` strings in logger). Also removed duplicate newlines from output.
  type: fix
pr_number: 5195
