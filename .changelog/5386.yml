changes:
- description: Add npm caching to the `actions/setup-node` CI step and increase the `validate-files-new-validate` job timeout to 90 minutes to fix intermittent failures caused by Node.js download time on cold runners.
  type: internal
pr_number: 5386
