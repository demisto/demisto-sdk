changes:
- description: Added support for routing `demistoextended` docker images to a private registry (GCR) via the `DEMISTO_SDK_EXTENDED_REGISTRY` environment variable.
  type: feature
pr_number: 5412
