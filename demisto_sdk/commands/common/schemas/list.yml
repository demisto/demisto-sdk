type: map
mapping:
  allRead:
    type: bool
  allReadWrite:
    type: bool
  data:
    type: str
  dbotCreatedBy:
    type: str
  description:
    type: str
  fromVersion:
    type: str
    required: true
  hasRole:
    type: bool
  id:
    type: str
    required: true
  itemVersion:
    type: str
    required: true
  locked:
    type: bool
    required: true
  name:
    type: str
    required: true
  nameLocked:
    type: bool
    required: true
  packID:
    type: str
    required: true
  previousAllRead:
    type: bool
  previousAllWrite:
    type: bool
  previousRoles:
    type: seq
    sequence:
    - includes: previousRoles_schema
  roles:
    type: seq
    sequence:
    - includes: roles_schema
  system:
    type: bool
    required: true
  tags:
    type: seq
    sequence:
    - type: str
  toVersion:
    type: str
  truncated:
    type: bool
  type:
    type: str
    required: true
  version:
    type: number
    required: true

  regex;(allRead:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(allReadWrite:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(data:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(dbotCreatedBy:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(description:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(fromVersion:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(hasRole:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(itemVersion:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(locked:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(name:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(nameLocked:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(packID:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(previousAllRead:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(previousAllWrite:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(previousRoles:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: seq
    sequence:
    - includes: previousRoles_schema
  regex;(roles:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: seq
    sequence:
    - includes: roles_schema
  regex;(system:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(tags:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: seq
    sequence:
    - type: str
  regex;(toVersion:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(truncated:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(type:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(version:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: number
schema;previousRoles_schema:
  type: any

schema;roles_schema:
  type: any
