type: map
mapping:
  action:
    type: str
    required: true
  enabled:
    type: bool
    required: true
  existingEventsFilters:
    type: seq
    sequence:
    - include: existingEventsFilters_schema
  fromVersion:
    type: str
    required: true
  id:
    type: str
    required: true
  index:
    type: number
    required: true
  itemVersion:
    type: str
    required: true
  linkTo:
    type: str
    required: true
  locked:
    type: bool
    required: true
  name:
    type: str
    required: true
  description:
    type: str
    required: false
  newEventFilters:
    type: seq
    sequence:
    - include: newEventFilters_schema
  packID:
    type: str
    required: true
  period:
    type: seq
    sequence:
    - include: period_schema
  readyExistingEventsFilters:
    type: seq
    sequence:
    - include: readyExistingEventsFilters_schema
  readyNewEventFilters:
    type: seq
    sequence:
    - include: readyNewEventFilters_schema
  scriptName:
    type: number
    required: true
  searchClosed:
    type: bool
    required: true
  system:
    type: bool
    required: true
  toServerVersion:
    type: str
    required: true
  version:
    type: number
    required: true

  regex;(action:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(enabled:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(existingEventsFilters:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: seq
    sequence:
    - include: existingEventsFilters_schema
  regex;(fromVersion:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(index:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: number
  regex;(itemVersion:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(linkTo:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(locked:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(name:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(description:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(newEventFilters:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: seq
    sequence:
    - include: newEventFilters_schema
  regex;(packID:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(period:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: seq
    sequence:
    - include: period_schema
  regex;(readyExistingEventsFilters:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: seq
    sequence:
    - include: readyExistingEventsFilters_schema
  regex;(readyNewEventFilters:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: seq
    sequence:
    - include: readyNewEventFilters_schema
  regex;(scriptName:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: number
  regex;(searchClosed:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(system:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: bool
  regex;(toServerVersion:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: str
  regex;(version:(xsoar)|(marketplacev2)|(xpanse)|(xsoar_on_prem)|(xsoar_saas)):
    type: number
schema;existingEventsFilters_schema:
  type: any

schema;newEventFilters_schema:
  type: any

schema;period_schema:
  type: any

schema;readyExistingEventsFilters_schema:
  type: any

schema;readyNewEventFilters_schema:
  type: any
